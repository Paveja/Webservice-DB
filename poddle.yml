poddle:
  version: "1.0"
  project: Smart Moderation Ai
  appName: chatbot-service
  type: WEB_SERVICE
  health: /health
  port: 8080
  env:
    preview:
      entryPoint: "Program.cs"
      specs:
        compute:
          cpu: 2
          memory: 2
          storage: 30
        database:
          cpu: 2
          memory: 1
          storage: 30
          engine: postgres
          version: "16"
      variables:
        BASE_URL: /api/v1
        DATABASE_URL: "#{dbUrl}"
        DATABASE_PORT: "#{dbPort}"
        DATABASE_USERNAME: "#{dbUsername}"
        DATABASE_PASSWORD: "#{dbPassword}"
        DATABASE_NAME: users
        THIRD_PARTY_URL: https://testtt.requestcatcher.com/

